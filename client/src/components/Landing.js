import React from "react";

const Landing = () => {
  return (<div style={{
      textAlign: "center"
    }}>
    <h1>Queue Reader!</h1>
    Collects real time message from Kafka.!
  </div>);
};

export default Landing;
